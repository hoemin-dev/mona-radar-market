import test from"node:test";import assert from"node:assert/strict";import{DatabaseSync}from"node:sqlite";import{MIGRATIONS}from"./migrations.js";import{migrateMarketDatabase}from"./database.js";
test("v17 to v18 preserves existing bid award and contract rows",()=>{const db=new DatabaseSync(":memory:");db.exec("PRAGMA foreign_keys=ON");for(const migration of MIGRATIONS.filter(x=>x.version<=17)){db.exec(migration.sql);db.exec(`PRAGMA user_version=${migration.version}`);}db.exec(`INSERT INTO api_raw_item(service,operation,item_sha256,canonical_json,parser_version,first_seen_at)VALUES('BidPublicInfoService','getBidPblancListInfoThngPPSSrch','aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa','{}','t','t');INSERT INTO bid_notice(bid_ntce_no,bid_ntce_ord,source_raw_item_id,source_operation,semantic_row_hash,semantic_state_json,parse_warnings_json,first_normalized_at,last_normalized_at)VALUES('B1','000',1,'getBidPblancListInfoThngPPSSrch','bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb','{}','[]','t','t')`);const before=(db.prepare("SELECT count(*) n FROM bid_notice").get()as{n:number}).n;migrateMarketDatabase(db);migrateMarketDatabase(db);assert.equal((db.prepare("PRAGMA user_version").get()as{user_version:number}).user_version,18);assert.equal((db.prepare("SELECT count(*) n FROM bid_notice").get()as{n:number}).n,before);for(const table of["bid_enrichment_state","bid_license_limit","bid_participation_region","bid_notice_change_event","bid_eorder_attachment"]){const sql=(db.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name=?").get(table)as{sql:string}).sql;assert.match(sql,/STRICT\s*$/u);}assert.equal((db.prepare("PRAGMA integrity_check").get()as{integrity_check:string}).integrity_check,"ok");assert.deepEqual(db.prepare("PRAGMA foreign_key_check").all(),[]);db.close();});
