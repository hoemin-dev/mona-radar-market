import type{DatabaseSync}from"node:sqlite";import type{KonepsResponse}from"../koneps/types.js";import{CONTRACT_SEARCH_OPERATION}from"../koneps/endpoints.js";import{CONTRACT_OPERATION,CONTRACT_SERVICE}from"../normalization/contract.js";import{normalizeContractRawItem,type ContractWriteAction}from"../normalization/contract-repository.js";import{persistRawPage,startCollectorRun,startOperationRun}from"../storage/raw-persistence.js";
export interface ContractClient{request(operation:typeof CONTRACT_SEARCH_OPERATION,params:any):Promise<KonepsResponse>}
export async function collectContractRange(o:{database:DatabaseSync;client:ContractClient;target:string;targetName:string;range:{start:string;end:string};isCancelled?:()=>boolean;onProgress?:(p:{page:number;totalCount:number;processed:number})=>void}){const at=()=>new Date().toISOString(),runId=startCollectorRun(o.database,{mode:"period",requestedRangeStart:o.range.start,requestedRangeEnd:o.range.end,startedAt:at(),appVersion:"0.1.0",parserVersion:"contract-v1"}),operationRunId=startOperationRun(o.database,{runId,service:CONTRACT_SERVICE,operation:CONTRACT_OPERATION,queryBasis:`contract_date+prdctClsfcNoNm:${o.target}`,effectiveRangeStart:o.range.start,effectiveRangeEnd:o.range.end,startedAt:at()});let page=1,total=0,processed=0;const counts:Record<ContractWriteAction,number>={inserted:0,updated:0,unchanged:0};try{do{if(o.isCancelled?.())break;const date=(value:string)=>value.slice(0,10).replace(/-/g,""),params={pageNo:page,numOfRows:100,type:"json"as const,inqryDiv:"1"as const,inqryBgnDate:date(o.range.start),inqryEndDate:date(o.range.end),prdctClsfcNoNm:o.targetName},response=await o.client.request(CONTRACT_SEARCH_OPERATION,params),saved=persistRawPage(o.database,{operationRunId,service:CONTRACT_SERVICE,operation:CONTRACT_OPERATION,requestedAt:response.metadata.startedAt,completedAt:response.receivedAt,durationMs:response.durationMs,httpStatus:response.status,resultCode:response.envelope.resultCode,resultMsg:response.envelope.resultMsg,pageNo:page,numOfRows:100,totalCount:response.envelope.totalCount??0,requestMetadata:params,requestUrl:response.metadata.redactedUrl,responseBytes:response.bodyBytes,contentType:response.headers["content-type"],encoding:"utf-8",parsedJson:response.parsedJson,parserVersion:"contract-v1"});total=response.envelope.totalCount??saved.actualItemCount;for(const rawId of saved.rawItemIds){const result=normalizeContractRawItem(o.database,rawId,o.target,at());counts[result.action]++;processed++;}o.onProgress?.({page,totalCount:total,processed});page++;}while(processed<total);const status=o.isCancelled?.()?"cancelled":"succeeded";o.database.prepare("UPDATE collector_operation_run SET status=?,completed_at=?,inserted_count=?,updated_count=?,unchanged_count=? WHERE operation_run_id=?").run(status,at(),counts.inserted,counts.updated,counts.unchanged,operationRunId);o.database.prepare("UPDATE collector_run SET status=?,completed_at=?,inserted_count=?,updated_count=?,unchanged_count=? WHERE run_id=?").run(status,at(),counts.inserted,counts.updated,counts.unchanged,runId);return{runId,status,total,processed,counts};}catch(error){o.database.prepare("UPDATE collector_operation_run SET status='failed',completed_at=?,error_summary='redacted contract collection error' WHERE operation_run_id=?").run(at(),operationRunId);o.database.prepare("UPDATE collector_run SET status='failed',completed_at=?,error_summary='redacted contract collection error' WHERE run_id=?").run(at(),runId);throw error;}}
