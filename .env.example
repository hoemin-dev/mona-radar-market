# Backend/collector runtime only. Never prefix this secret with VITE_.
KONEPS_SERVICE_KEY=
# preserve: keep an already encoded key unchanged; encode: encode a decoded key once.
KONEPS_SERVICE_KEY_MODE=preserve
